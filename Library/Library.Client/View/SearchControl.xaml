<UserControl x:Class="Library.Client.View.SearchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:view="clr-namespace:Library.Client.View"
             xmlns:viewModel="clr-namespace:Library.Client.ViewModel"
             mc:Ignorable="d"  d:DataContext="{d:DesignInstance viewModel:SearchViewModel}">
    <UserControl.Resources>
        <view:IndexConverter x:Key="IndexConverter" />
        <view:PagesConverter x:Key="PagesConverter" />
    </UserControl.Resources>
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="141*"/>
            <RowDefinition Height="34*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="1"
              HorizontalAlignment="Stretch" VerticalAlignment="Center" Width="Auto"  Margin="1" Height="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="Поиск" Margin="10" HorizontalAlignment="Left" Height="30"  Width="100" VerticalAlignment="Center" Command="{Binding SearchCommand}"/>
            <Button Content="Добавить" Margin="10" HorizontalAlignment="Right" Height="30" Width="100" Grid.Column="2" Command="{Binding AddBookCommand}"/>
            <Button Content="Получить все"  Margin="10" HorizontalAlignment="Center" Height="30"  Width="100" VerticalAlignment="Center" Grid.Column="1" Command="{Binding GetAllBooksCommand}"/>
        </Grid>
        <TextBox Margin="10,10,9.6,10" 
                 TextWrapping="Wrap" 
                 Grid.Row="0" Grid.Column="0"
                 HorizontalAlignment="Stretch" 
                 Height="20" 
                 Width="Auto" 
                 VerticalAlignment="Center"
                 Text="{Binding SearchPhrase, Mode=OneWayToSource}" />
        <ScrollViewer Margin="1" Grid.Row="2" RenderTransformOrigin="0.5,0.5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto" CanContentScroll="True">
            <ListView Height="Auto" Width="Auto" ItemsSource="{Binding Path=FoundedBooks}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, 
                                        Converter={StaticResource IndexConverter}}"
                                        Width="30">№</GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Path=Name}" Width="200">Название</GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Path=Pages, Converter={StaticResource PagesConverter}}" 
                                        Width="100">Страницы</GridViewColumn>
                    </GridView>
                </ListView.View>
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Открыть"
                                  CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ContextMenu}, 
                              Path=PlacementTarget.SelectedItem}" 
                                  Command="{Binding OpenBookCommand}"/>
                        <MenuItem Header="Удалить" 
                                  CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ContextMenu}, 
                              Path=PlacementTarget.SelectedItem}" 
                                  Command="{Binding DeleteBookCommand}"/>
                        <MenuItem Header="Открыть Highlights"
                                  CommandParameter="{Binding RelativeSource={RelativeSource 
                                  AncestorType=ContextMenu}, Path=PlacementTarget.SelectedItem}"
                                  Command="{Binding OpenBookHighlightsCommand}"></MenuItem>
                    </ContextMenu>
                </ListView.ContextMenu>
            </ListView>
        </ScrollViewer>
        <ScrollViewer HorizontalAlignment="Stretch" Height="70" Margin="1" Grid.Row="3" VerticalAlignment="Bottom" MinHeight="55">
            <TextBlock Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"  HorizontalAlignment="Stretch" Margin="10" Grid.Row="3" TextWrapping="Wrap" VerticalAlignment="Bottom" Height="50"/>
        </ScrollViewer>
    </Grid>
</UserControl>
