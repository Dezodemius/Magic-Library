<UserControl x:Class="Library.Client.View.HighlightsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:view="clr-namespace:Library.Client.View"
             xmlns:viewModel="clr-namespace:Library.Client.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance viewModel:HighlightsViewModel}">
    <UserControl.Resources>
        <view:IndexConverter x:Key="IndexConverter" />
        <view:HighlightConverter x:Key="HighlightConverter" />
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto" CanContentScroll="True">
        <ListView ItemsSource="{Binding Path=SelectedBook}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}},
                                    Converter={StaticResource IndexConverter}}"
                                    Width="30">№
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Highlight, Converter={StaticResource HighlightConverter}}"
                                    Width="600">Найденный текст
                    
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" />
                                    <TextBlock Text="{Binding Description}" />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Page}" 
                                    Width="30">Номер страницы
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </ScrollViewer>
</UserControl>
